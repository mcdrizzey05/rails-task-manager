<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>

<p><%= link_to "Modify this task", edit_task_path(@task.id) %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
